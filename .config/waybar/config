{
	"position": "top",
	"height": 20,
	"modules-left": [
		"sway/workspaces",
		"sway/mode",
		"sway/window"
	],
	"modules-right": [
		"cpu",
		"memory",
		"network",
		"pulseaudio",
		"custom/mic",
		"clock",
		"custom/dwm",
		"tray"
	],
	"sway/workspaces": {
		"disable-scroll": true,
		"disable-markup": true,
		"format": "{name}"
	},
	"sway/window": {
		"format": "{}",
		"max-length": 150,
		"max-height": 20,
	},
	"cpu": {
		"format": "󰻠 {usage}%",
		"interval": 1
	},
	"memory": {
		"format": "󰍛 {percentage}%",
		"interval": 1
	},
	"tray": {
		"icon-size": 18,
		"spacing": 5
	},
	"clock": {
		"format": "{:%Y-%m-%d %H:%M:%S}",
		"interval": 1
	},
	"network": {
		"format-ethernet": "󰈀 Connected",
		"format-wifi": " Connected",
		"format-disconnected": "Disconnected",
		"tooltip-format": "{ifname} {ipaddr}",
		"interval": 10
	},
	"pulseaudio": {
		"format": "{icon} {volume}%",
		"format-muted": "󰝟 Mute",
		"interval": 1,
		"format-icons": {
			"default": [
				""
			]
		},
		"on-click": "pactl set-sink-mute @DEFAULT_SINK@ toggle",
		"on-scroll-up": "pactl set-sink-volume @DEFAULT_SINK@ +5%",
		"on-scroll-down": "pactl set-sink-volume @DEFAULT_SINK@ -5%",
		"on-click-middle": "pavucontrol-qt"
	},
	"custom/mic": {
		"format": "{}",
		"exec": "pactl get-source-mute @DEFAULT_SOURCE@ | sed 's/Mute: //;s/yes//;s/no//'",
		"exec-if": "sleep 0.1",
		"exec-on-event": true,
		"tooltip": false,
		"interval": 1,
		"on-click": "pactl set-source-mute @DEFAULT_SOURCE@ toggle"
	},
	"custom/dwm": {
		"exec": "dwm -v 2>&1; true",
		"format": "{}",
		"once": true
	}
}
